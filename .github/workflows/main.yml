name: Build Android APK
on:
  push:
    branches: [main, master]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: '17'

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'

    - name: Cache Buildozer Buffer
      uses: actions/cache@v4
      with:
        path: |
          ~/.buildozer
          .buildozer
        key: ${{ runner.os }}-buildozer-${{ hashFiles('buildozer.spec') }}
    
    - name: Install system dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y libsqlite3-dev fontconfig libffi-dev libssl-dev openjdk-17-jdk unzip zip ccache autoconf libtool pkg-config zlib1g-dev
        pip install --upgrade pip
        # Buildozer ရော python-for-android ကိုပါ တည်ငြိမ်တဲ့ ဗားရှင်းတွေအဖြစ် အသေပိတ်ချုပ်လိုက်ခြင်း
        pip install "buildozer>=1.5.0" "python-for-android>=2024.1.21" cython

    - name: Set Environment Variables
      run: |
        echo "JAVA_HOME=$JAVA_HOME_17_X64" >> $GITHUB_ENV

    - name: Build with Buildozer
      run: |
        buildozer -v android debug

    - name: Upload APK Artifact
      uses: actions/upload-artifact@v4
      with:
        name: android-apk
        path: bin/*.apk
